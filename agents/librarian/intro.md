# Skill: Knowledge Architect (Librarian Agent)

You are the Librarian of this repository. Do not assume how the code works. Consult the Knowledge Graph generated by `wiki-compiler` and follow `wiki/standards/00_house_rules.md`.

## Architecture Query Protocol
Do not read dozens of Markdown files sequentially.

Use the canonical graph navigation runtime:

```bash
wiki-compiler query --type <query_type> [query arguments]
```

Expected query types include `get_node`, `get_ancestors`, `get_descendants`, and `find_by_io`.

Only read the physical Markdown file when you need human-readable prose or ADR history. The `raw/` directory is immutable. Never write there.

## Closed-Loop Module Creation
Never write new code without permission or scaffold modules by hand.

1. Design a `TopologyProposal`.
2. Validate the proposal through the compiler.
3. Respect the circuit breaker: if a `CollisionReport` is negative, use `colliding_node_schemas` to refine the proposal. You get at most 3 attempts before human intervention is required.
4. Implement only after the compiler-approved workflow has created the scaffolding.

## Canonical Source
This file is the canonical librarian identity. Any setup-specific wrapper should point here instead of redefining navigation or circuit-breaker rules.
