---
identity:
  node_id: doc:wiki/drafts/reglas_de_interacci_n.md
  node_type: concept
edges:
- target_id: raw:raw/unimplemented_from_sourcetalk.md
  relation_type: documents
compliance:
  status: implemented
  failing_standards: []
source:
  source_path: raw/unimplemented_from_sourcetalk.md
  source_hash: 1a7f8c9ba485c0342c7bddb0d133479345f1edd3e7047103e0544db195914f61
  compiled_at: '2026-04-14T16:50:28.666429'
  compiled_from: wiki-compiler
---

- **Si el usuario quiere crear un feature nuevo:** Busca nodos con `status="planned"` en el `ComplianceFacet` para ver si ya hay trabajo previo, o pide crear la entrada en `future_docs/` primero.

## Definition

- **Si el usuario quiere crear un feature nuevo:** Busca nodos con `status="planned"` en el `ComplianceFacet` para ver si ya hay trabajo previo, o pide crear la entrada en `future_docs/` primero.

## Examples

- **Si el usuario quiere crear un feature nuevo:** Busca nodos con `status="planned"` en el `ComplianceFacet` para ver si ya hay trabajo previo, o pide crear la entrada en `future_docs/` primero.
- **Respuestas técnicas:** Basa tus respuestas en el `ASTFacet` (firmas reales) y `SemanticFacet` (intenciones documentadas).

## Related Concepts

- [[Index]]
- [[core_philosophy]]

## Details

- **Si el usuario quiere crear un feature nuevo:** Busca nodos con `status="planned"` en el `ComplianceFacet` para ver si ya hay trabajo previo, o pide crear la entrada en `future_docs/` primero.
- **Respuestas técnicas:** Basa tus respuestas en el `ASTFacet` (firmas reales) y `SemanticFacet` (intenciones documentadas).
```

---

Generated from `raw/unimplemented_from_sourcetalk.md`.
