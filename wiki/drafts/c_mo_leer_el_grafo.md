---
identity:
  node_id: doc:wiki/drafts/c_mo_leer_el_grafo.md
  node_type: concept
edges:
- target_id: raw:raw/unimplemented_from_sourcetalk.md
  relation_type: documents
compliance:
  status: implemented
  failing_standards: []
source:
  source_path: raw/unimplemented_from_sourcetalk.md
  source_hash: 1a7f8c9ba485c0342c7bddb0d133479345f1edd3e7047103e0544db195914f61
  compiled_at: '2026-04-14T16:50:28.666386'
  compiled_from: wiki-compiler
---

- Todo es un `KnowledgeNode` con una `SystemIdentity` única (ej. `file:src/ai/graph.py`).

## Definition

- Todo es un `KnowledgeNode` con una `SystemIdentity` única (ej.

## Examples

- Todo es un `KnowledgeNode` con una `SystemIdentity` única (ej. `file:src/ai/graph.py`).
- Las dependencias se expresan en la lista `edges`. Busca `relation_type="depends_on"` o `reads_from` para trazar el impacto de un cambio.
- Nunca inventes flujos de datos. Si un nodo no tiene un `IOFacet`, asume que no realiza I/O o que el parser aún no lo ha escaneado.

## Related Concepts

- [[Index]]
- [[core_philosophy]]

## Details

- Todo es un `KnowledgeNode` con una `SystemIdentity` única (ej. `file:src/ai/graph.py`).
- Las dependencias se expresan en la lista `edges`. Busca `relation_type="depends_on"` o `reads_from` para trazar el impacto de un cambio.
- Nunca inventes flujos de datos. Si un nodo no tiene un `IOFacet`, asume que no realiza I/O o que el parser aún no lo ha escaneado.

Generated from `raw/unimplemented_from_sourcetalk.md`.
