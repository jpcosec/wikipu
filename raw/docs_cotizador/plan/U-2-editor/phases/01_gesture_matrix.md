# Phase 01 - Gesture Matrix and Baseline Inventory

## Context

Before implementing drag behaviors, we need a complete interaction inventory from:

- legacy editor behavior (`claps_codelab/Components_Timeline.html`),
- timeline draft (`plan/legacy/I-3-category/html_playground_draft.html`),
- current rebuild runtime events.

## Objectives

- [ ] Produce `plan/U-2-editor/gesture_event_matrix.md`.
- [ ] Tag each interaction as `existing`, `to-add`, or `deferred`.
- [ ] Validate that each non-deferred interaction maps to existing runtime events.
- [ ] Mark group/kit interactions explicitly as deferred.

## Acceptance

- Matrix reviewed and complete.
- No interaction is left without event mapping or deferral note.

## Commit

`docs(plan): define U-2 gesture matrix and baseline`
